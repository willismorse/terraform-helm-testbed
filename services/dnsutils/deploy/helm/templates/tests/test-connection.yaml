apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dnsutils.fullname" . }}-test-connection"
  labels:
    {{- include "dnsutils.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dnsutils.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
