apiVersion: v1
kind: Service
metadata:
  name: {{ include "dnsutils.fullname" . }}
  labels:
    {{- include "dnsutils.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dnsutils.selectorLabels" . | nindent 4 }}
